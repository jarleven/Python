Pi SenseHAT documentation

```
sudo apt install -y sense-hat
sudo apt install -y python3-pip
sudo apt install -y vim

python3 -m pip install --upgrade --user pip 

rm textScrollLoop.py
wget https://raw.githubusercontent.com/jarleven/Python/master/PiSenseHAT/textScrollLoop.py

python3 textScrollLoop.py

```
